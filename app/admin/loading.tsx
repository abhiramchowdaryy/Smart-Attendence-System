import { PageSkeleton } from "@/components/page-skeleton";

/** Covers every route in this section (Next.js applies the nearest one). */
export default function Loading() {
  return <PageSkeleton />;
}
